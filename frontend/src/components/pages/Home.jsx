// Personalized recommendation feed
// TODO: call /api/recommend with stored ratings, render movie poster grid
export default function Home() {
  return <div style={{ padding: '2rem' }}>Home — your personalised picks appear here</div>
}
