// Genre radar chart + rating history + taste summary
// TODO: call /api/taste-profile, render recharts RadarChart
export default function TasteProfile() {
  return <div style={{ padding: '2rem' }}>Taste Profile — your genre breakdown appears here</div>
}
